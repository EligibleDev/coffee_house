import QueryTitle from "../QueryTitle/QueryTitle";

const PriceRange = () => {
    // TODO: take the range picker from here: https://react.keepdesign.io/docs/components/slider

    return (
        <div>
            <QueryTitle>Filter by price</QueryTitle>
        </div>
    );
};

export default PriceRange;
